@model IEnumerable<OurDestination.Models.Month>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MonthName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MonthName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MonthId }) |
            @Html.ActionLink("Details", "Details", new { id=item.MonthId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MonthId })
        </td>
    </tr>
}

</table>
